name: Release
on:
  push:
    # Only RTM-marked tags create a public release. Plain version tags
    # (v0.1.9, v0.2.0 etc.) are kept as repository markers but no longer
    # auto-publish — kernalix7 found that rapid-iteration tags between
    # v0.1.9 and v0.2.2.1 shipped unverified releases that broke users'
    # working installs (2026-04-29). The "RTM" suffix is the explicit
    # opt-in: e.g. `v0.2.3-RTM`, `v0.3.0-RTM1`. Manual workflow_dispatch
    # still works for emergency one-offs.
    tags:
      - "*RTM*"
  workflow_dispatch:

permissions:
  contents: write
  # id-token: write  # reserved for future PyPI trusted publisher

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: pip install ruff
      - name: Run ruff check
        run: ruff check src/ tests/
      - name: Run ruff format check
        run: ruff format --check src/ tests/
      - name: Install project
        run: pip install -e ".[dev]"
      - name: Run tests
        run: pytest tests/ -v
      # Skip tag/pyproject version parity check on manual dispatch
      - name: Verify tag matches pyproject version
        if: github.event_name == 'push'
        run: |
          # Strip leading 'v' and trailing '-RTM*' so 'v0.2.3-RTM' compares
          # against pyproject's '0.2.3'.
          tag="${GITHUB_REF_NAME#v}"
          tag="${tag%%-RTM*}"
          pyver=$(python -c "import tomllib,pathlib; print(tomllib.loads(pathlib.Path('pyproject.toml').read_text())['project']['version'])")
          if [ "$tag" != "$pyver" ]; then
            echo "Tag version ($tag) does not match pyproject.toml version ($pyver)" >&2
            exit 1
          fi

  build:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install build twine
      - name: Build distribution
        run: python -m build
      - name: Check distribution metadata
        run: python -m twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Extract changelog section
        id: changelog
        run: |
          tag="${GITHUB_REF_NAME#v}"
          tag="${tag%%-RTM*}"
          notes=$(awk -v ver="$tag" '
            $0 ~ "^## \\[" ver "\\]" { flag=1; next }
            flag && /^## \[/ { exit }
            flag { print }
          ' CHANGELOG.md)
          if [ -z "$(echo "$notes" | tr -d '[:space:]')" ]; then
            notes="See CHANGELOG.md for details."
          fi
          {
            echo "notes<<CHANGELOG_EOF"
            echo "$notes"
            echo "CHANGELOG_EOF"
          } >> "$GITHUB_OUTPUT"
      - name: Detect prerelease
        id: prerelease
        run: |
          tag="${GITHUB_REF_NAME}"
          if echo "$tag" | grep -qE -- '-(rc|beta|alpha)'; then
            echo "value=true" >> "$GITHUB_OUTPUT"
          else
            echo "value=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ steps.prerelease.outputs.value }}
          body: ${{ steps.changelog.outputs.notes }}
          files: |
            dist/*.tar.gz
            dist/*.whl
